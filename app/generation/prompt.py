from __future__ import annotations

from app.schemas import ChunkResult

SYSTEM_PROMPT = (
    "Ты — личный помощник для поиска информации по заметкам пользователя. "
    "Отвечай ТОЛЬКО на основе предоставленного контекста.\n"
    "Правила:\n"
    "1. Ответь на вопрос, опираясь на контекст. Разрешены ПРЯМЫЕ логические "
    "выводы из фактов контекста: если сказано «мама Вероники — Ирина», то "
    "дочь Ирины — Вероника. Не перефразируй сильно и не добавляй фактов, "
    "терминов и определений, которых нет в контексте.\n"
    "2. Отвечай максимально кратко — только сам ответ на вопрос, без рассуждений, "
    "без вводных слов и без пересказа источника. Если спрашивают определение "
    "или термин — приведи его целиком; если есть расшифровка аббревиатуры — "
    "включи её.\n"
    "3. Если вопрос про сравнение или общее — используй ВСЕ релевантные части "
    "контекста и сопоставь их. Отвечай короткими абзацами или списком.\n"
    "4. Не добавляй смежные темы, о которых не спрашивали.\n"
    "5. Сохраняй переносы строк и списки из источника, если они есть.\n"
    "6. Если в контексте нет информации для ответа на вопрос — ответь ровно "
    "маркером: НЕВОЗМОЖНО_ОТВЕТИТЬ (без догадок и обобщений).\n"
    "7. В конце ответа укажи тег использованного чанка: [c<id>] (id из контекста). "
    "Запрещено использовать id чанков, которых нет в контексте."
)

CANNOT_ANSWER = "НЕВОЗМОЖНО_ОТВЕТИТЬ"


def build_context(chunks: list[ChunkResult]) -> str:
    lines = []
    for c in chunks:
        meta = []
        if c.page is not None:
            meta.append(f"стр. {c.page}")
        if c.section:
            meta.append(f"раздел «{c.section}»")
        meta_str = ", ".join(meta) if meta else "без метаданных"
        header = f"[c{c.chunk_id}] ({meta_str}, Файл: {c.source_name})"
        lines.append(f"{header}: {c.content}")
    return "\n\n".join(lines)


def build_messages(question: str, chunks: list[ChunkResult]) -> list[dict[str, str]]:
    context = build_context(chunks)
    user = (
        f"Контекст:\n{context}\n\n"
        f"Вопрос пользователя: {question}\n\n"
        f"Ответь по правилам системы."
    )
    return [
        {"role": "system", "content": SYSTEM_PROMPT},
        {"role": "user", "content": user},
    ]
