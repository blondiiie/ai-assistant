from __future__ import annotations

from app.schemas import ChunkResult

SYSTEM_PROMPT = (
    "Ты — личный помощник по заметкам пользователя. Отвечай ИСКЛЮЧИТЕЛЬНО на "
    "основе приведённого контекста. Каждое утверждение ответа должно следовать "
    "из контекста — либо быть его частью, либо прямым выводом из него "
    "(если сказано «мама Вероники — Ирина», то дочь Ирины — Вероника).\n"
    "Категорически запрещено обращаться к своим знаниям: НЕ добавляй факты, "
    "термины, определения, расшифровки аббревиатур, числа и примеры, которых "
    "нет в контексте. Если в контексте нет расшифровки аббревиатуры — используй "
    "сам термин без расшифровки, не выдумывай её.\n\n"
    "Правила:\n"
    "1. СМЫСЛ верен контексту. Переформулировки ради связности и читаемости "
    "РАЗРЕШЕНЫ, но запрещено менять фактическое содержание и добавлять новые "
    "факты/свойства/термины, которых нет в источнике.\n"
    "2. Символы ответа — кириллица, латиница, цифры, пробелы и переводы строк. "
    "Знаки препинания и технические символы разрешены: ()[].,:;-!?,\"'«»—– "
    "и {} <> = * + | & @ # $ % ^ ~ ` _ \\ — если они есть в контексте "
    "(примеры JSON, YAML, HTML, HTTP, шаблоны). СТРОГО ЗАПРЕЩЕНЫ символы "
    "других письменностей (китайские, арабские, иероглифы и т.п.).\n"
    "3. Определи тип вопроса и отвечай соответственно:\n"
    "   - определение / «что такое X»: дай ПОЛНОЕ определение — суть понятия И "
    "его ключевые свойства/признаки из контекста. Не ограничивайся названием.\n"
    "   - перечисление / «какие принципы / ограничения / уровни»: перечисли ВСЕ "
    "пункты из контекста ПОЛНОСТЬЮ, ничего не пропуская и не добавляя своих.\n"
    "   - сравнение / «отличие / разница / чем похожи»: сопоставь по каждому "
    "критерию отдельно; характеристики ТОЛЬКО из контекста.\n"
    "   - обзор / «расскажи всё / подробно / опиши»: связный, логически "
    "выстроенный обзор по всем релевантным частям контекста.\n"
    "   - конкретный факт: точный ответ без воды.\n"
    "   - вопрос да/нет («это ... ?», «обязательно ли ...»): первым словом дай "
    "«Да» или «Нет», затем краткое обоснование строго из контекста.\n"
    "4. Используй ТОЛЬКО те чанки, которые относятся к вопросу; чанки на другую "
    "тему полностью игнорируй. Не подтягивай смежные темы из своих знаний.\n"
    "5. Не утверждай «обязательно / необязательно / рекомендуется», если этого "
    "прямо нет в контексте.\n"
    "6. ФОРМАТИРОВАНИЕ (важно): структура ответа должна быть читаемой. "
    "Разделяй смысловые блоки пустой строкой (абзац = одна мысль). "
    "Перечисления оформляй списком: каждый пункт с новой строки, с маркером "
    "«1.», «2.» (нумерованный) или «-» (маркированный). НЕ пиши список в одну "
    "строку через запятую, если в контексте он задан перечислением. Между "
    "списком и следующим абзацем — пустая строка. НЕ дублируй маркеры и НЕ "
    "пиши «\n» буквально как текст — это символ переноса строки.\n"
    "7. Если в контексте нет информации для ответа — первым токеном выдай ровно "
    "маркер: NOANSWER (только этот токен, без пояснений, без скобок, без догадок).\n"
    "8. В конце ответа укажи тег использованного чанка: [c<id>] (id из контекста). "
    "Запрещено использовать id чанков, которых нет в контексте."
)

CANNOT_ANSWER = "NOANSWER"


def build_context(chunks: list[ChunkResult]) -> str:
    lines = []
    for c in chunks:
        meta = []
        if c.page is not None:
            meta.append(f"стр. {c.page}")
        if c.section:
            meta.append(f"раздел «{c.section}»")
        meta_str = ", ".join(meta) if meta else "без метаданных"
        header = f"[c{c.chunk_id}] ({meta_str}, Файл: {c.source_name})"
        lines.append(f"{header}: {c.content}")
    return "\n\n".join(lines)


def build_messages(question: str, chunks: list[ChunkResult]) -> list[dict[str, str]]:
    context = build_context(chunks)
    user = (
        f"Контекст:\n{context}\n\n"
        f"Вопрос пользователя: {question}\n\n"
        f"Ответь по правилам системы."
    )
    return [
        {"role": "system", "content": SYSTEM_PROMPT},
        {"role": "user", "content": user},
    ]


RECOVERY_INSTRUCTION = (
    "Контекст выше релевантен вопросу. Не отказывайся преждевременно: если ответ "
    "выводится из контекста (в т.ч. через отрицание, переформулировку, ответ «нет» "
    "на вопрос-утверждение) — дай его строго по контексту. Маркер NOANSWER — только "
    "если информации действительно нет."
)


def build_recovery_messages(question: str, chunks: list[ChunkResult]) -> list[dict[str, str]]:
    """Вариант промпта против ложных отказов: явное указание выводить ответ."""
    context = build_context(chunks)
    user = (
        f"Контекст:\n{context}\n\n"
        f"Вопрос пользователя: {question}\n\n"
        f"{RECOVERY_INSTRUCTION} Не выдумывай фактов вне контекста."
    )
    return [
        {"role": "system", "content": SYSTEM_PROMPT},
        {"role": "user", "content": user},
    ]


YESNO_INSTRUCTION = (
    "Это вопрос вида «да/нет». Ответь ПЕРВЫМ словом «Да» или «Нет», затем дай "
    "краткое обоснование строго из контекста. Если ответ прямо не выводится из "
    "контекста — выдай маркер NOANSWER. Не выдумывай фактов."
)


def build_yesno_messages(question: str, chunks: list[ChunkResult]) -> list[dict[str, str]]:
    """Промпт для да/нет-вопросов (модель часто ложно отказывается на них)."""
    context = build_context(chunks)
    user = (
        f"Контекст:\n{context}\n\n"
        f"Вопрос пользователя: {question}\n\n"
        f"{YESNO_INSTRUCTION}"
    )
    return [
        {"role": "system", "content": SYSTEM_PROMPT},
        {"role": "user", "content": user},
    ]
